// if guest vm(devbox) is up?
// todos
// 1. figure out how to shell into host(windows) from node
// 2. confirm shell node client is working with host(windows)
// 3. export a method that can run given command into host(windows)

// if website containers are up?
// todos
// 1. confirm ssh into guest vm(devbox)
// 2. confirm ssh node client is working with guest vm(devbox)
// 3. export a method that can run given command into guest vm(devbox)
